server:
  port: 9025

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sachinrana135/emeritus
          clone-on-start: true